First, install the 'nltk' package by running the following command in your Python environment:

```python
!pip install nltk
import nltk
```
For printing, run the code at `mini-gpt/generation/random_token.py`.
